export function useUserMedia(requestedMedia, setBlob) {
    async function enableStream() {
      try {
        
      } catch(err) {
        // Removed for brevity
      }
    }
  enableStream();
  return mediaStream;
}
